{
  "name": "fluorite-demo-ts",
  "version": "0.1.0",
  "description": "TypeScript demo for Fluorite code generation with multi-package imports",
  "main": "dist/index.js",
  "scripts": {
    "generate": "fluorite ts --inputs ../demo/fluorite --output ./generated",
    "build": "tsc",
    "start": "ts-node src/index.ts",
    "demo": "npm run generate && npm run build && node dist/index.js"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "ts-node": "^10.9.0",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "@zhxiaogg/fluorite-cli": "file:../../npm/fluorite-cli"
  }
}
